---
title: QOJ 3433
date: 2026-04-19 18:00:00 +0900
categories:
- PS
tags:
- 알고리즘, 수학, 베타적논리합기저 gf(2)
description: Xor Maximazation
usemathjax: true
---

## 링크

https://qoj.ac/contest/371/problem/3433

## 사용 태그

- 베타적 논리합 기저 gf(2)

## 풀이

XOR로 만들 수 있는 모든 값을 생각하자. 그걸 만들긴 위해서는 최소 숫자 집합이 필요한데, 이를 XOR 기저라고 한다.  
어떤 숫자가 다른 숫자들의 XOR로 만들어지면 필요가 없고, 절대 못 만들면 기저에 포함 시키면 된다.  
GF(2)에서는, 덧셈이 XOR이라서 그냥 XOR로 하는 선형대수라고 보면 편하다.

숫자 하나씩 보면서 기존 기저로 XOR해서 최대한 줄이고, 다음과 같이 하면 된다.
- 0이 되었다 : 이미 만들 수 있으니 버려야한다
- 0이 아니면 : 새 정보니까 추가해야한다.

이 문제는 XOR로 만들 수 있는 최대값을 구하는 거니까, XOR 기저를 만들기만 하면 된다.

## 코드

```c
#include <stdio.h>
#include <math.h>

long long basis[60];
int sz = 0;

void add(long long x) {
    for(int i = 0; i < sz; i++) if ((x ^ basis[i]) < x) x ^= basis[i];
    if (x != 0) basis[sz++] = x;
}

int main() {
    int n;
    scanf("%d", &n);
    for(int i = 0; i < n; i++) {
        long long x;
        scanf("%lld", &x);
        add(x);
    }

    for(int i = 0; i < sz; i++) {
        for(int j = i + 1; j < sz; j++) {
            if (basis[i] < basis[j]) {
                long long tmp = basis[i];
                basis[i] = basis[j];
                basis[j] = tmp;
            }
        }
    }

    long long res = 0;
    for(int i = 0; i < sz; i++) if ((res ^ basis[i]) > res) res ^= basis[i];
    printf("%lld", res);
}
```